general:
  artifacts:
    - ".coverage-html/"

machine:
  python:
    version: 3.3.3

dependencies:
  pre:
    - pip install -r requirements-dev.txt

test:
  override:
    - py.test --cov-report html --cov atria tests
