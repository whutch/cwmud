general:
  artifacts:
    - ".coverage-html/"

machine:
  python:
    version: 3.3.3

dependencies:
  pre:
    - pip install -r tests/requirements.txt
    - pip install coveralls

test:
  override:
    - py.test --cov-report html --pep8 tests --pep8 atria --cov atria
  post:
    - coveralls
