general:
  artifacts:
    - ".coverage-html/"

machine:
  python:
    version: 3.3.3

dependencies:
  pre:
    - pip install -r requirements-dev.txt
    - pip install coveralls

test:
  override:
    - py.test --cov-report html --cov atria tests
  post:
    - coveralls
