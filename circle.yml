general:
  artifacts:
    - ".coverage-html/"

machine:
  python:
    version: 3.4.2

dependencies:
  pre:
    - pip install -r requirements.txt
    - pip install -r tests/requirements.txt
    - pip install coveralls

test:
  override:
    - make tests
    - make coverage
  post:
    - coveralls
